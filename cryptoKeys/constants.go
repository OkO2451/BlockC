package cryptoKeys

const (
	// Length of the private key
	PrivateKeyLength = 64
	// Length of the public key
	PublicKeyLength = 32

	// length of the seed
	SeedLength = 32

	// Length of the signature
	SignatureLength = 64

	// Length of the address
	AddressLength = 20
)
